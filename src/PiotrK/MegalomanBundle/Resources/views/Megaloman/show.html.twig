{% extends '::base.html.twig' %}

{% block body -%}
  <h3>Megaloman {{entity}}</h3>

  {{ include("MegalomanBundle:Discography:discography-table.html.twig" , {'discography': entity.discography} )}}

  <ul class="list-unstyled">
    <li>
      <a href="{{ path('megaloman') }}">
        Wróć do listy megalomanów
      </a>
    </li>
    <li>
      <a href="{{ path('megaloman_edit', { 'id': entity.id }) }}">
        Edytuj
      </a>
    </li>
    <li>{{ form(delete_form) }}</li>
  </ul>
{% endblock %}
