{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Discography list</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Właściciel</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('discography_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{entity.owner}}</td>
                <td>
                <ul class="list-unstyled">
                    <li>
                        <a href="{{ path('discography_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('discography_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {% endblock %}
